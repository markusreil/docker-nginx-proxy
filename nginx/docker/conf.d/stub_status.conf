server {
    listen 8080;
    location = /stub_status {
        stub_status;
        allow 172.16.0.0/12;   # Docker networks (incl. web-proxy)
        allow 192.168.0.0/16;  # LAN
        deny all;
        access_log off;
    }
}